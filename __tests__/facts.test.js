const request = require('supertest');
const app = require('../lib/app');
const { getFact, getFacts } = require('../db/db-helpers');

describe('fact routes', () => {
  it('gets all facts', async() => {
  
  })  
});


//FACT ROUTES

//GET routes for facts


